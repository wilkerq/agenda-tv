rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura pública de eventos, mas apenas escrita por usuários autenticados.
    match /events/{eventId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
