rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /events/{document=**} {
      // Temporary diagnostic rule: Allow all reads and writes
      allow read, write: if true;
    }
  }
}
