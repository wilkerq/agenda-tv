rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite que qualquer pessoa leia os eventos, mas apenas usuários autenticados escrevam.
    match /events/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
